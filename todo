add the exit msg, when user ctrl + c
finish the command feature.

cached :     private static String[] COMMANDS = {
        "ping", "server_info", "list"
    };

check that out : https://javaalmanac.io/snippets/security/diffiehellmankeyagreement/

    private static final String[] COMMANDS = { 
        "list", "show", "ping"
    };

